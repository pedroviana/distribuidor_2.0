<%# @admin_user.hidden_update = true %>
<%= semantic_form_for [:admin, current_admin_user], :url => admin_minha_contum_path do |f| %>
	<%= f.inputs "Detalhes" do %>	
		<%= f.input :name %>
		<%= f.input :password, as: :password %>
		<%= f.input :password_confirmation, as: :password %>
	<% end -%>

	<%= f.actions %>
<% end %>

